#ifndef PIPE_H
#define PIPE_H

/**
 * @brief This function is used to execute piped commands
 *
 * @param input: The input string containing the piped commands
 */
void execute_piped_commands(char* input);

#endif // PIPE_H
